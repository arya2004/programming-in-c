/**
 * Adds the values pointed to by two pointers and stores the result in the first pointer.
 * @param a - Pointer to the first integer.
 * @param b - Pointer to the second integer.
 */
void ptr_ad(int *a, int *b) {
    *a += *b;
}
